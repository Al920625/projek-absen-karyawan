{
  "name": "projek-absen-karyawan",
  "version": "1.0.0",
  "description": "Employee attendance system project",
  "scripts": {
    "build": "npm run build:functions",
    "build:functions": "firebase deploy --only functions",
    "start": "firebase emulators:start",
    "test": "jest"
  }
}
